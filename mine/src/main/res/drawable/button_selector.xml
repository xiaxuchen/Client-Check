<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:tools="http://schemas.android.com/tools" xmlns:android="http://schemas.android.com/apk/res/android" >
    <item android:state_pressed="false">
        <shape xmlns:android="http://schemas.android.com/apk/res/android"
            android:shape="rectangle">
            <corners android:radius="5dp" />
            <!-- 圆角深红色按钮 -->
            <solid android:color="@color/common_primary_color"/>
            <padding
                android:bottom="5dp"
                android:top="5dp"
                android:left="5dp"
                android:right="5dp"></padding>
            <!--设置边框线的宽度和颜色-->
            <stroke android:width="1dp" android:color="@color/common_primary_color" />
        </shape>
    </item>

    <item android:state_pressed="true">
        <shape xmlns:android="http://schemas.android.com/apk/res/android"
            android:shape="rectangle">
            <!-- 圆角红色按钮 -->
            <solid android:color="#1296db"/>
            <corners android:radius="5dp"/>
            <!--设置边框线的宽度和颜色-->
            <stroke android:width="8dp" android:color="#1296db" />
        </shape>
    </item>

</selector>