<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cxyz.check">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/common_logo"
        tools:replace="android:icon,android:label"
        android:supportsRtl="true"
        android:theme="@style/CheckTheme"
        android:name="com.cxyz.logiccommons.application.MyApp">

        <activity android:name=".activity.DailyCheckActivity" >

        </activity>

        <activity android:name=".activity.MyHistoryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.CheckHistoryActivity" />
        <activity android:name=".activity.AlterCheckActivity">
        </activity>

        <activity android:name=".activity.OtherstateActivity"/>
    </application>

</manifest>